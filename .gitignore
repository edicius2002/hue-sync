# Entorno virtual
.venv/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# CREDENCIALES DEL BRIDGE. Contiene el clientkey (PSK del handshake DTLS).
# Nunca debe versionarse. La plantilla es hue_config.example.json.
hue_config.json

# Grabaciones de calibracion
*.wav

# Editores
.vscode/
.idea/

# Estado de runtime de CCB: worktrees de agentes, sondas y memoria compartida.
# Nunca se versiona. Ademas queda fuera del lint: son ficheros de usar y tirar
# y hacian fallar `ruff check .`, que es el comando de verificacion del repo.
.ccb/
